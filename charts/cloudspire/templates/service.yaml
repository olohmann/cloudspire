apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudspire.fullname" . }}
  labels:
    {{- include "cloudspire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudspire.selectorLabels" . | nindent 4 }}
